# -*- coding: utf-8 -*-
"""Titanic data Analysis

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1FB-F5XnVjk_LFnbxfSSFQE3moTrq1EoF
"""

import pandas as pd
import numpy as np

df = pd.read_csv("/content/drive/MyDrive/Colab Notebooks/Titanic-Dataset.csv")

PassengerId = df["PassengerId"].to_numpy()
print(PassengerId)

survived_series = df["Survived"]
print("Survived Series (first 5 rows):\n", survived_series.head())

Pclass = df["Pclass"].to_numpy()
print(Pclass)

name = df["Name"].to_numpy()
print(name)

sex = df["Sex"].to_numpy()
print(sex)

age_series = df["Age"]
print("Age Series (first 5 rows):\n", age_series.head())

sib = df["SibSp"].to_numpy()
print(sib)

parch = df["Parch"].to_numpy()
print(parch)

ticket = df["Ticket"].to_numpy()
print(ticket)

fare_series = df["Fare"]
print("Fare Series (first 5 rows):\n", fare_series.head())

print("DataFrame head():")
display(df.head())

print("\nDataFrame info():")
df.info()

print("\nDataFrame describe():")
display(df.describe())

cabin = df["Cabin"].to_numpy()
print(cabin)

embarked = df["Embarked"].to_numpy()
print(embarked)



print("Missing values before handling:")
display(df.isnull().sum())

df['Age'].fillna(df['Age'].mean(), inplace=True)
df['Embarked'].fillna(df['Embarked'].mode()[0], inplace=True)
print("Missing values after handling:")
display(df.isnull().sum())

"""The `Cabin` column still has a large number of missing values. Since there are too many missing values in this column, it's best to drop it. Dropping the `Cabin` column will remove it from the DataFrame."""

df.drop('Cabin', axis=1, inplace=True)
print("Missing values after dropping 'Cabin' column:")
display(df.isnull().sum())

# Survival rate by Gender
survival_by_gender = df.groupby('Sex')['Survived'].mean().reset_index()
print("Survival Rate by Gender:")
display(survival_by_gender)

# Average Age by Passenger Class
average_age_by_pclass = df.groupby('Pclass')['Age'].mean().reset_index()
print("\nAverage Age by Passenger Class:")
display(average_age_by_pclass)

# Survival count by Embarked location
survival_count_by_embarked = df.groupby('Embarked')['Survived'].sum().reset_index()
print("\nSurvival Count by Embarked Location:")
display(survival_count_by_embarked)

"""# Task
Create a new DataFrame with `Pclass` and `ClassDescription` and merge it with the main DataFrame, then create another DataFrame with `Sex` and `GenderDescription` and join it with a subset of the main DataFrame. Finally, create a small DataFrame of new passengers and concatenate it to the main DataFrame, summarizing the outcomes of these merge, join, and concatenation operations.

## Create Additional DataFrame

### Subtask:
Create a new Pandas DataFrame containing a 'Pclass' column and a 'ClassDescription' column, which will be used for merging and joining with the main DataFrame.

**Reasoning**:
The subtask requires creating a new DataFrame `class_description_df` with 'Pclass' and 'ClassDescription' columns, populating them with specified values, and then displaying the DataFrame. This can be achieved using a single code block to define the data and construct the DataFrame.
"""

data = {'Pclass': [1, 2, 3],
        'ClassDescription': ['First Class', 'Second Class', 'Third Class']}
class_description_df = pd.DataFrame(data)

print("Created Class Description DataFrame:")
display(class_description_df)

"""# Task
Calculate the average age of survivors versus non-survivors, find the highest and lowest fare, calculate the overall survival percentage, and summarize the identified patterns affecting survival from the Titanic dataset.

## Calculate Average Age of Survivors vs. Non-Survivors

### Subtask:
Group the DataFrame by the 'Survived' column and calculate the mean 'Age' for both groups (survivors and non-survivors). Display the result.

**Reasoning**:
The subtask requires grouping the DataFrame by the 'Survived' column and calculating the mean of the 'Age' for each group, then printing the result. I will use the `groupby()` and `mean()` methods to achieve this.
"""

average_age_by_survival = df.groupby('Survived')['Age'].mean()
print("Average Age by Survival Status:")
print(average_age_by_survival)

"""## Find Highest and Lowest Fare

### Subtask:
Find the highest and lowest fare from the 'Fare' column of the DataFrame. Display both values.

**Reasoning**:
To find the highest and lowest fare, I will use the `max()` and `min()` methods directly on the 'Fare' column of the DataFrame. This will provide the required values efficiently.
"""

highest_fare = df['Fare'].max()
lowest_fare = df['Fare'].min()

print(f"Highest Fare: {highest_fare}")
print(f"Lowest Fare: {lowest_fare}")

"""## Calculate Overall Survival Percentage

### Subtask:
Calculate the overall survival percentage from the 'Survived' column of the DataFrame. Display the result as a percentage.

**Reasoning**:
To calculate the overall survival percentage, I will compute the mean of the 'Survived' column and multiply by 100, then display the result.
"""

overall_survival_percentage = df['Survived'].mean() * 100
print(f"Overall Survival Percentage: {overall_survival_percentage:.2f}%")

"""## Summarize Identified Patterns Affecting Survival

### Subtask:
Summarize the identified patterns affecting survival from the Titanic dataset, drawing conclusions from the previously calculated statistics (survival rate by gender, average age by survival, and overall survival percentage).

**Summary of Survival Patterns:**
Based on the analysis performed:
*   **Gender:** Females had a significantly higher survival rate compared to males (as seen in the 'survival_by_gender' calculation).
*   **Age:** The average age of survivors (around 28.34 years) was slightly lower than that of non-survivors (around 30.63 years), suggesting that younger individuals or children might have had a marginally better chance, although this difference is not large.
*   **Overall Survival:** The overall survival rate was approximately 38.38%, indicating that less than half of the passengers survived the disaster.

## Final Task

### Subtask:
Provide a summary of all calculations and identified patterns affecting survival.

## Summary:

### Q&A
1.  **What is the average age of survivors versus non-survivors?**
    The average age of survivors was approximately 28.34 years, while the average age of non-survivors was approximately 30.63 years.
2.  **What are the highest and lowest fares?**
    The highest fare paid was \$512.33, and the lowest fare paid was \$0.0.
3.  **What is the overall survival percentage?**
    The overall survival rate was 38.38%.
4.  **What are the identified patterns affecting survival?**
    Females had a significantly higher survival rate compared to males. Survivors were, on average, slightly younger than non-survivors.

### Data Analysis Key Findings
*   The average age of non-survivors was approximately 30.63 years, which is slightly higher than the average age of survivors, at approximately 28.34 years.
*   The highest fare paid by a passenger was \$512.33, while the lowest fare paid was \$0.0.
*   The overall survival rate for passengers on the Titanic was 38.38%.
*   Gender was a significant factor in survival, with females having a considerably higher survival rate than males.

### Insights or Next Steps
*   Further analysis could explore the fare distribution more deeply, especially why some fares were \$0.0, to understand its potential impact on survival or passenger class.
*   Investigate other factors like passenger class and port of embarkation, and their interaction with age and gender, to build a more comprehensive model of survival probability.
"""